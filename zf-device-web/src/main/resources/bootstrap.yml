spring:
  application:
    name: device-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:18848
      config:
        server-addr: 127.0.0.1:18848
        group: DEFAULT_GROUP
        file-extension: yml
        shared-configs:
          - dataId: druid.yml
            refresh: true
          - dataId: mysql.yml
            refresh: true
          - dataId: redis.yml
            refresh: true
          - dataId: kafka-producer.yml
            refresh: true
          - dataId: ribbon-feign-hystrix.yml
            refresh: true


